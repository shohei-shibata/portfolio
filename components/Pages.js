const Pages = props => (
  <div className="section-container light flex-container-col">
    <section className="top-content">
      { props.children } 
    </section>		
  </div>
);

export default Pages;
